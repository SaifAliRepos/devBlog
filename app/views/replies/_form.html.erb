<%= form_with(model: reply.comment) do |form| %>
  <% if reply.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reply.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% reply.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :reply, style: "display: block" %>
    <%= form.text_field :reply, required: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
